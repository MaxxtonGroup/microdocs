{
  "dependencies": {
    "{dependencyTitle}": {
      "paths": {
        "{path}": {
          "{method}": {
            "~~~#": "Remove response body when the field filter is set",
            "~~~IF": {
              "condition": "scope.parameters.filter(function(param){return param.name === 'fields';}).length > 0",
              "then": "scope.responses = {}"
            }
          }
        }
      }
    }
  }
}