
export const storage = {
  staticFolder: "storage.staticFolder",
  driver: "storage.driver",
  yaml: {
    settingsFile: "storage.yaml.settingsFile",
    scriptsFolder: "storage.yaml.scriptsFolder",
    projectsFolder: "storage.yaml.projectsFolder",
    reportsFolder: "storage.yaml.reportsFolder"
  },
  json: {
    settingsFile: "storage.json.settingsFile",
    scriptsFolder: "storage.json.scriptsFolder",
    projectsFolder: "storage.json.projectsFolder",
    reportsFolder: "storage.json.reportsFolder"
  }
};

export const server = {
  port: "server.port"
};