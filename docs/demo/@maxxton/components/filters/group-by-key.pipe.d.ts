/**
 * Filter list of cast by argument
 * Usage:
 * string | stringFilter: 'mustContain': 'mustNotContain'
 * Example:
 *   url(assignment) | groupByKey: '/'
 *   assignment | groupByKey: ''
 */
export declare class GroupByKeyPipe {
    private transform(list, key);
}
